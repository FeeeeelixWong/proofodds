# Use one network consistently for API, program ID, IDL and RPC.
TXLINE_NETWORK=devnet
TXLINE_API_TOKEN=
SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional. Defaults are selected from TXLINE_NETWORK.
# TXLINE_API_ORIGIN=https://txline-dev.txodds.com
